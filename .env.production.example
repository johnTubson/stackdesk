# Copy to .env.production on the server.
# Generate a secret: openssl rand -base64 32

DATABASE_URL=file:./data/local.db
SESSION_SECRET=replace-with-openssl-rand-base64-32
NODE_ENV=production

# Optional — only needed for HTTPS via Caddy (docker compose --profile https)
# DOMAIN=stackdesk.com
